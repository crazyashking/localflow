* text=auto
*.py text diff=python
*.md text
*.toml text
*.txt text
*.wav binary
# Without this, `* text=auto` above is one bad heuristic away from rewriting
# line endings inside a model file and corrupting it on checkout.
*.onnx binary
